This library contains a set of functions for writing data to/reading data from HDF5 files.  They are implemented on an even higher-level than h5py functions, requiring the user to know very little about the HDF5 format to use them.  The functionality encompassed by this library is that which is most useful when writing classes to interface with the modred library.