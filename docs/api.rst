Full API
========

The API described below is accessible from the top-level namespace of ``h5io``.
That is, even though methods are listed below with names like ``h5io.h5io.load_dataset``, they can be called as ``h5io.load_dataset``.

.. automodule:: h5io.h5io
   :members:
